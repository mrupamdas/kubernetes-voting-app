apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
spec:
  type: NodePort
  ports:
    - port: 8080
      name: "result-services"
      targetPort: 80
      nodePort: 31001
  selector:
    app: result
     